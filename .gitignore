# Private match/stream config — do not commit; only the encoded HTML is pushed
matches-config.json

# Secrets (MPD URLs, auth headers, keys) — never commit
.env
.env.local
.env.*.local

# Node (if you add other scripts later)
node_modules/
